import { FC } from "react";

const Search : FC = () : JSX.Element => {
    return (
        <div>
            <input />
            <button>Szukaj</button>
        </div>
    );
};

export default Search;
